export const perfetctReport = (arr) => {
  let minValue = arr[0].dt
  let minIndex = 0

  const d = new Date()

  weaterArray.forEach((report, index) => {
    const currentTime = d.getTime()

    console.log(currentTime)
  })

  //   console.log(minIndex)
}
